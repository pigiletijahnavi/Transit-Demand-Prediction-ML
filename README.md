# Transit-Demand-Prediction-ML
A Machine Learning project for predicting transit demand using Random Forest, Gradient Boosting, and XGBoost models — including data preprocessing, model evaluation, hyperparameter tuning, and cross-validation for optimized accuracy.
